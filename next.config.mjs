/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "kegucop4fdrwb2jw.public.blob.vercel-storage.com",
      // اگر دامنه دیگه‌ای هم داری، اینجا اضافه کن
    ],
  },
};

export default nextConfig;
